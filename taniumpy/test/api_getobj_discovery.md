# Generated by api_getobj_discovery.py
## Action

```

>>> # findall non list
>>> a=api.Action()
>>> r=session.find(a)
>>> print r
EXCEPTION:  ERROR: 400 Bad RequestXML Parse Error: SOAPProcessing Exception: class ActionNotFound

>>> # find by id non list
>>> a=api.Action()
>>> a.id=1
>>> r=session.find(a)
>>> print r
Action, name: 'Unmanaged Asset Tracking - Run Scan'

>>> # find by name non list
>>> a=api.Action()
>>> a.id=1
>>> r1=session.find(a)
>>> print r1
>>> b=api.Action()
>>> b.name=r1.name
>>> r2=session.find(b)
>>> print r2
Action, name: 'Unmanaged Asset Tracking - Run Scan'
EXCEPTION:  ERROR: 400 Bad RequestXML Parse Error: SOAPProcessing Exception: class ActionNotFound

>>> # findall list
>>> a=api.ActionList()
>>> r=session.find(a)
>>> print r
ActionList, len: 709

>>> # find by id in list
>>> a=api.Action()
>>> a.id=1
>>> b=api.ActionList()
>>> b.append(a)
>>> r=session.find(a)
>>> print r
Action, name: 'Unmanaged Asset Tracking - Run Scan'

>>> # find by name in list
>>> a=api.Action()
>>> a.id=1
>>> r1=session.find(a)
>>> print r1
>>> b=api.Action()
>>> b.name=r1.name
>>> c=api.ActionList()
>>> c.append(b)
>>> r2=session.find(c)
>>> print r2
Action, name: 'Unmanaged Asset Tracking - Run Scan'
ActionList, len: 709
```

## ActionListInfo

```

>>> # findall non list
>>> a=api.ActionListInfo()
>>> r=session.find(a)
>>> print r
None
```

## ActionStop

```

>>> # findall non list
>>> a=api.ActionStop()
>>> r=session.find(a)
>>> print r
EXCEPTION:  ERROR: 400 Bad RequestXML Parse Error: SOAP Parsing Exception: TagNotFound: action

>>> # find by id non list
>>> a=api.ActionStop()
>>> a.id=1
>>> r=session.find(a)
>>> print r
EXCEPTION:  ERROR: 400 Bad RequestXML Parse Error: SOAPProcessing Exception: class ActionStopNotFound

>>> # findall list
>>> a=api.ActionStopList()
>>> r=session.find(a)
>>> print r
None

>>> # find by id in list
>>> a=api.ActionStop()
>>> a.id=1
>>> b=api.ActionStopList()
>>> b.append(a)
>>> r=session.find(a)
>>> print r
EXCEPTION:  ERROR: 400 Bad RequestXML Parse Error: SOAPProcessing Exception: class ActionStopNotFound
```

## ArchivedQuestion

```

>>> # findall non list
>>> a=api.ArchivedQuestion()
>>> r=session.find(a)
>>> print r
None

>>> # find by id non list
>>> a=api.ArchivedQuestion()
>>> a.id=1
>>> r=session.find(a)
>>> print r
None

>>> # findall list
>>> a=api.ArchivedQuestionList()
>>> r=session.find(a)
>>> print r
None

>>> # find by id in list
>>> a=api.ArchivedQuestion()
>>> a.id=1
>>> b=api.ArchivedQuestionList()
>>> b.append(a)
>>> r=session.find(a)
>>> print r
None
```

## AuditData

```

>>> # findall non list
>>> a=api.AuditData()
>>> r=session.find(a)
>>> print r
None
```

## CacheFilter

```

>>> # findall non list
>>> a=api.CacheFilter()
>>> r=session.find(a)
>>> print r
None

>>> # findall list
>>> a=api.CacheFilterList()
>>> r=session.find(a)
>>> print r
None
```

## CacheInfo

```

>>> # findall non list
>>> a=api.CacheInfo()
>>> r=session.find(a)
>>> print r
None
```

## ClientCount

```

>>> # findall non list
>>> a=api.ClientCount()
>>> r=session.find(a)
>>> print r
ClientCount
```

## ClientStatus

```

>>> # findall non list
>>> a=api.ClientStatus()
>>> r=session.find(a)
>>> print r
SystemStatusList, len: 6
```

## ComputerGroup

```

>>> # findall non list
>>> a=api.ComputerGroup()
>>> r=session.find(a)
>>> print r
None

>>> # find by id non list
>>> a=api.ComputerGroup()
>>> a.id=1
>>> r=session.find(a)
>>> print r
None

>>> # find by name non list
>>> a=api.ComputerGroup()
>>> a.id=1
>>> r1=session.find(a)
>>> print r1
>>> b=api.ComputerGroup()
>>> b.name=r1.name
>>> r2=session.find(b)
>>> print r2
None
EXCEPTION:  'NoneType' object has no attribute 'name'

>>> # findall list
>>> a=api.ComputerGroupList()
>>> r=session.find(a)
>>> print r
None

>>> # find by id in list
>>> a=api.ComputerGroup()
>>> a.id=1
>>> b=api.ComputerGroupList()
>>> b.append(a)
>>> r=session.find(a)
>>> print r
None

>>> # find by name in list
>>> a=api.ComputerGroup()
>>> a.id=1
>>> r1=session.find(a)
>>> print r1
>>> b=api.ComputerGroup()
>>> b.name=r1.name
>>> c=api.ComputerGroupList()
>>> c.append(b)
>>> r2=session.find(c)
>>> print r2
None
EXCEPTION:  'NoneType' object has no attribute 'name'
```

## ComputerGroupSpec

```

>>> # findall non list
>>> a=api.ComputerGroupSpec()
>>> r=session.find(a)
>>> print r
None

>>> # find by id non list
>>> a=api.ComputerGroupSpec()
>>> a.id=1
>>> r=session.find(a)
>>> print r
None
```

## ComputerSpecList

```

>>> # findall non list
>>> a=api.ComputerSpecList()
>>> r=session.find(a)
>>> print r
None
```

## ErrorList

```

>>> # findall non list
>>> a=api.ErrorList()
>>> r=session.find(a)
>>> print r
None
```

## Filter

```

>>> # findall non list
>>> a=api.Filter()
>>> r=session.find(a)
>>> print r
None

>>> # find by id non list
>>> a=api.Filter()
>>> a.id=1
>>> r=session.find(a)
>>> print r
None

>>> # findall list
>>> a=api.FilterList()
>>> r=session.find(a)
>>> print r
None

>>> # find by id in list
>>> a=api.Filter()
>>> a.id=1
>>> b=api.FilterList()
>>> b.append(a)
>>> r=session.find(a)
>>> print r
None
```

## Group

```

>>> # findall non list
>>> a=api.Group()
>>> r=session.find(a)
>>> print r
None

>>> # find by id non list
>>> a=api.Group()
>>> a.id=1
>>> r=session.find(a)
>>> print r
Group, name: 'All Computers'

>>> # find by name non list
>>> a=api.Group()
>>> a.id=1
>>> r1=session.find(a)
>>> print r1
>>> b=api.Group()
>>> b.name=r1.name
>>> r2=session.find(b)
>>> print r2
Group, name: 'All Computers'
Group, name: 'All Computers'

>>> # findall list
>>> a=api.GroupList()
>>> r=session.find(a)
>>> print r
GroupList, len: 3

>>> # find by id in list
>>> a=api.Group()
>>> a.id=1
>>> b=api.GroupList()
>>> b.append(a)
>>> r=session.find(a)
>>> print r
Group, name: 'All Computers'

>>> # find by name in list
>>> a=api.Group()
>>> a.id=1
>>> r1=session.find(a)
>>> print r1
>>> b=api.Group()
>>> b.name=r1.name
>>> c=api.GroupList()
>>> c.append(b)
>>> r2=session.find(c)
>>> print r2
Group, name: 'All Computers'
GroupList, len: 1
```

## MetadataItem

```

>>> # findall non list
>>> a=api.MetadataItem()
>>> r=session.find(a)
>>> print r
None

>>> # find by name non list
>>> a=api.MetadataItem()
>>> a.id=1
>>> r1=session.find(a)
>>> print r1
>>> b=api.MetadataItem()
>>> b.name=r1.name
>>> r2=session.find(b)
>>> print r2
None
EXCEPTION:  'NoneType' object has no attribute 'name'
```

## MetadataList

```

>>> # findall non list
>>> a=api.MetadataList()
>>> r=session.find(a)
>>> print r
None
```

## ObjectList

```

>>> # findall non list
>>> a=api.ObjectList()
>>> r=session.find(a)
>>> print r
None
```

## Options

```

>>> # findall non list
>>> a=api.Options()
>>> r=session.find(a)
>>> print r
None
```

## PackageFile

```

>>> # findall non list
>>> a=api.PackageFile()
>>> r=session.find(a)
>>> print r
None

>>> # find by id non list
>>> a=api.PackageFile()
>>> a.id=1
>>> r=session.find(a)
>>> print r
None

>>> # find by name non list
>>> a=api.PackageFile()
>>> a.id=1
>>> r1=session.find(a)
>>> print r1
>>> b=api.PackageFile()
>>> b.name=r1.name
>>> r2=session.find(b)
>>> print r2
None
EXCEPTION:  'NoneType' object has no attribute 'name'

>>> # find by hash non list
>>> a=api.PackageFile()
>>> a.id=1
>>> r1=session.find(a)
>>> print r1
>>> b=api.PackageFile()
>>> b.hash=r1.hash
>>> r2=session.find(b)
>>> print r2
None
EXCEPTION:  'NoneType' object has no attribute 'hash'

>>> # findall list
>>> a=api.PackageFileList()
>>> r=session.find(a)
>>> print r
None

>>> # find by id in list
>>> a=api.PackageFile()
>>> a.id=1
>>> b=api.PackageFileList()
>>> b.append(a)
>>> r=session.find(a)
>>> print r
None

>>> # find by name in list
>>> a=api.PackageFile()
>>> a.id=1
>>> r1=session.find(a)
>>> print r1
>>> b=api.PackageFile()
>>> b.name=r1.name
>>> c=api.PackageFileList()
>>> c.append(b)
>>> r2=session.find(c)
>>> print r2
None
EXCEPTION:  'NoneType' object has no attribute 'name'
```

## PackageFileStatus

```

>>> # findall non list
>>> a=api.PackageFileStatus()
>>> r=session.find(a)
>>> print r
None

>>> # findall list
>>> a=api.PackageFileStatusList()
>>> r=session.find(a)
>>> print r
None
```

## PackageFileTemplate

```

>>> # findall non list
>>> a=api.PackageFileTemplate()
>>> r=session.find(a)
>>> print r
None

>>> # find by name non list
>>> a=api.PackageFileTemplate()
>>> a.id=1
>>> r1=session.find(a)
>>> print r1
>>> b=api.PackageFileTemplate()
>>> b.name=r1.name
>>> r2=session.find(b)
>>> print r2
None
EXCEPTION:  'NoneType' object has no attribute 'name'

>>> # find by hash non list
>>> a=api.PackageFileTemplate()
>>> a.id=1
>>> r1=session.find(a)
>>> print r1
>>> b=api.PackageFileTemplate()
>>> b.hash=r1.hash
>>> r2=session.find(b)
>>> print r2
None
EXCEPTION:  'NoneType' object has no attribute 'hash'

>>> # findall list
>>> a=api.PackageFileTemplateList()
>>> r=session.find(a)
>>> print r
None

>>> # find by name in list
>>> a=api.PackageFileTemplate()
>>> a.id=1
>>> r1=session.find(a)
>>> print r1
>>> b=api.PackageFileTemplate()
>>> b.name=r1.name
>>> c=api.PackageFileTemplateList()
>>> c.append(b)
>>> r2=session.find(c)
>>> print r2
None
EXCEPTION:  'NoneType' object has no attribute 'name'
```

## PackageSpec

```

>>> # findall non list
>>> a=api.PackageSpec()
>>> r=session.find(a)
>>> print r
PackageSpecList, len: 88

>>> # find by id non list
>>> a=api.PackageSpec()
>>> a.id=1
>>> r=session.find(a)
>>> print r
PackageSpec, name: 'Update Java 64-bit - Kill / Reboot'

>>> # find by name non list
>>> a=api.PackageSpec()
>>> a.id=1
>>> r1=session.find(a)
>>> print r1
>>> b=api.PackageSpec()
>>> b.name=r1.name
>>> r2=session.find(b)
>>> print r2
PackageSpec, name: 'Update Java 64-bit - Kill / Reboot'
PackageSpec, name: 'Update Java 64-bit - Kill / Reboot'

>>> # findall list
>>> a=api.PackageSpecList()
>>> r=session.find(a)
>>> print r
None

>>> # find by id in list
>>> a=api.PackageSpec()
>>> a.id=1
>>> b=api.PackageSpecList()
>>> b.append(a)
>>> r=session.find(a)
>>> print r
PackageSpec, name: 'Update Java 64-bit - Kill / Reboot'

>>> # find by name in list
>>> a=api.PackageSpec()
>>> a.id=1
>>> r1=session.find(a)
>>> print r1
>>> b=api.PackageSpec()
>>> b.name=r1.name
>>> c=api.PackageSpecList()
>>> c.append(b)
>>> r2=session.find(c)
>>> print r2
PackageSpec, name: 'Update Java 64-bit - Kill / Reboot'
None
```

## Parameter

```

>>> # findall non list
>>> a=api.Parameter()
>>> r=session.find(a)
>>> print r
None

>>> # findall list
>>> a=api.ParameterList()
>>> r=session.find(a)
>>> print r
None
```

## ParseJob

```

>>> # findall non list
>>> a=api.ParseJob()
>>> r=session.find(a)
>>> print r
None

>>> # findall list
>>> a=api.ParseJobList()
>>> r=session.find(a)
>>> print r
None
```

## ParseResult

```

>>> # findall non list
>>> a=api.ParseResult()
>>> r=session.find(a)
>>> print r
None

>>> # find by id non list
>>> a=api.ParseResult()
>>> a.id=1
>>> r=session.find(a)
>>> print r
None

>>> # findall list
>>> a=api.ParseResultList()
>>> r=session.find(a)
>>> print r
None

>>> # find by id in list
>>> a=api.ParseResult()
>>> a.id=1
>>> b=api.ParseResultList()
>>> b.append(a)
>>> r=session.find(a)
>>> print r
None
```

## ParseResultGroup

```

>>> # findall non list
>>> a=api.ParseResultGroup()
>>> r=session.find(a)
>>> print r
None

>>> # findall list
>>> a=api.ParseResultGroupList()
>>> r=session.find(a)
>>> print r
None
```

## Permission

```

>>> # findall non list
>>> a=api.Permission()
>>> r=session.find(a)
>>> print r
EXCEPTION:  Permission instance has no attribute 'OBJECT_LIST_TAG'
```

## Plugin

```

>>> # findall non list
>>> a=api.Plugin()
>>> r=session.find(a)
>>> print r
EXCEPTION:  ERROR: 400 Bad RequestXML Parse Error: SOAPProcessing Exception: class PluginNotFound

>>> # find by name non list
>>> a=api.Plugin()
>>> a.id=1
>>> r1=session.find(a)
>>> print r1
>>> b=api.Plugin()
>>> b.name=r1.name
>>> r2=session.find(b)
>>> print r2
EXCEPTION:  ERROR: 400 Bad RequestXML Parse Error: SOAPProcessing Exception: class PluginNotFound

>>> # findall list
>>> a=api.PluginList()
>>> r=session.find(a)
>>> print r
PluginList, len: 0

>>> # find by name in list
>>> a=api.Plugin()
>>> a.id=1
>>> r1=session.find(a)
>>> print r1
>>> b=api.Plugin()
>>> b.name=r1.name
>>> c=api.PluginList()
>>> c.append(b)
>>> r2=session.find(c)
>>> print r2
EXCEPTION:  ERROR: 400 Bad RequestXML Parse Error: SOAPProcessing Exception: class PluginNotFound
```

## PluginArgument

```

>>> # findall non list
>>> a=api.PluginArgument()
>>> r=session.find(a)
>>> print r
None

>>> # find by name non list
>>> a=api.PluginArgument()
>>> a.id=1
>>> r1=session.find(a)
>>> print r1
>>> b=api.PluginArgument()
>>> b.name=r1.name
>>> r2=session.find(b)
>>> print r2
None
EXCEPTION:  'NoneType' object has no attribute 'name'

>>> # findall list
>>> a=api.PluginArgumentList()
>>> r=session.find(a)
>>> print r
None

>>> # find by name in list
>>> a=api.PluginArgument()
>>> a.id=1
>>> r1=session.find(a)
>>> print r1
>>> b=api.PluginArgument()
>>> b.name=r1.name
>>> c=api.PluginArgumentList()
>>> c.append(b)
>>> r2=session.find(c)
>>> print r2
None
EXCEPTION:  'NoneType' object has no attribute 'name'
```

## PluginCommand

```

>>> # findall non list
>>> a=api.PluginCommand()
>>> r=session.find(a)
>>> print r
EXCEPTION:  PluginCommand instance has no attribute 'OBJECT_LIST_TAG'

>>> # findall list
>>> a=api.PluginCommandList()
>>> r=session.find(a)
>>> print r
None
```

## PluginSchedule

```

>>> # findall non list
>>> a=api.PluginSchedule()
>>> r=session.find(a)
>>> print r
None

>>> # find by name non list
>>> a=api.PluginSchedule()
>>> a.id=1
>>> r1=session.find(a)
>>> print r1
>>> b=api.PluginSchedule()
>>> b.name=r1.name
>>> r2=session.find(b)
>>> print r2
None
EXCEPTION:  'NoneType' object has no attribute 'name'

>>> # findall list
>>> a=api.PluginScheduleList()
>>> r=session.find(a)
>>> print r
None

>>> # find by name in list
>>> a=api.PluginSchedule()
>>> a.id=1
>>> r1=session.find(a)
>>> print r1
>>> b=api.PluginSchedule()
>>> b.name=r1.name
>>> c=api.PluginScheduleList()
>>> c.append(b)
>>> r2=session.find(c)
>>> print r2
None
EXCEPTION:  'NoneType' object has no attribute 'name'
```

## PluginSql

```

>>> # findall non list
>>> a=api.PluginSql()
>>> r=session.find(a)
>>> print r
None
```

## PluginSqlColumn

```

>>> # findall non list
>>> a=api.PluginSqlColumn()
>>> r=session.find(a)
>>> print r
None

>>> # find by name non list
>>> a=api.PluginSqlColumn()
>>> a.id=1
>>> r1=session.find(a)
>>> print r1
>>> b=api.PluginSqlColumn()
>>> b.name=r1.name
>>> r2=session.find(b)
>>> print r2
None
EXCEPTION:  'NoneType' object has no attribute 'name'
```

## PluginSqlResult

```

>>> # findall non list
>>> a=api.PluginSqlResult()
>>> r=session.find(a)
>>> print r
None
```

## Question

```

>>> # findall non list
>>> a=api.Question()
>>> r=session.find(a)
>>> print r
QuestionList, len: 8257

>>> # find by id non list
>>> a=api.Question()
>>> a.id=1
>>> r=session.find(a)
>>> print r
Question, id: 1

>>> # find by name non list
>>> a=api.Question()
>>> a.id=1
>>> r1=session.find(a)
>>> print r1
>>> b=api.Question()
>>> b.name=r1.name
>>> r2=session.find(b)
>>> print r2
Question, id: 1
QuestionList, len: 8257

>>> # findall list
>>> a=api.QuestionList()
>>> r=session.find(a)
>>> print r
QuestionList, len: 8257

>>> # find by id in list
>>> a=api.Question()
>>> a.id=1
>>> b=api.QuestionList()
>>> b.append(a)
>>> r=session.find(a)
>>> print r
Question, id: 1

>>> # find by name in list
>>> a=api.Question()
>>> a.id=1
>>> r1=session.find(a)
>>> print r1
>>> b=api.Question()
>>> b.name=r1.name
>>> c=api.QuestionList()
>>> c.append(b)
>>> r2=session.find(c)
>>> print r2
Question, id: 1
EXCEPTION:  ERROR: 400 Bad RequestXML Parse Error: SOAPProcessing Exception: class QuestionNotFound
```

## QuestionListInfo

```

>>> # findall non list
>>> a=api.QuestionListInfo()
>>> r=session.find(a)
>>> print r
None
```

## SavedAction

```

>>> # findall non list
>>> a=api.SavedAction()
>>> r=session.find(a)
>>> print r
None

>>> # find by id non list
>>> a=api.SavedAction()
>>> a.id=1
>>> r=session.find(a)
>>> print r
SavedAction, name: 'Unmanaged Asset Tracking - Run Scan'

>>> # find by name non list
>>> a=api.SavedAction()
>>> a.id=1
>>> r1=session.find(a)
>>> print r1
>>> b=api.SavedAction()
>>> b.name=r1.name
>>> r2=session.find(b)
>>> print r2
SavedAction, name: 'Unmanaged Asset Tracking - Run Scan'
SavedAction, name: 'Unmanaged Asset Tracking - Deploy Scan Tools and Scan'

>>> # findall list
>>> a=api.SavedActionList()
>>> r=session.find(a)
>>> print r
SavedActionList, len: 22

>>> # find by id in list
>>> a=api.SavedAction()
>>> a.id=1
>>> b=api.SavedActionList()
>>> b.append(a)
>>> r=session.find(a)
>>> print r
SavedAction, name: 'Unmanaged Asset Tracking - Run Scan'

>>> # find by name in list
>>> a=api.SavedAction()
>>> a.id=1
>>> r1=session.find(a)
>>> print r1
>>> b=api.SavedAction()
>>> b.name=r1.name
>>> c=api.SavedActionList()
>>> c.append(b)
>>> r2=session.find(c)
>>> print r2
SavedAction, name: 'Unmanaged Asset Tracking - Run Scan'
SavedActionList, len: 1
```

## SavedActionApproval

```

>>> # findall non list
>>> a=api.SavedActionApproval()
>>> r=session.find(a)
>>> print r
None

>>> # find by id non list
>>> a=api.SavedActionApproval()
>>> a.id=1
>>> r=session.find(a)
>>> print r
None

>>> # find by name non list
>>> a=api.SavedActionApproval()
>>> a.id=1
>>> r1=session.find(a)
>>> print r1
>>> b=api.SavedActionApproval()
>>> b.name=r1.name
>>> r2=session.find(b)
>>> print r2
None
EXCEPTION:  'NoneType' object has no attribute 'name'
```

## SavedActionPolicy

```

>>> # findall non list
>>> a=api.SavedActionPolicy()
>>> r=session.find(a)
>>> print r
None
```

## SavedActionRowId

```

>>> # findall non list
>>> a=api.SavedActionRowId()
>>> r=session.find(a)
>>> print r
EXCEPTION:  SavedActionRowId instance has no attribute 'OBJECT_LIST_TAG'

>>> # findall list
>>> a=api.SavedActionRowIdList()
>>> r=session.find(a)
>>> print r
None
```

## SavedQuestion

```

>>> # findall non list
>>> a=api.SavedQuestion()
>>> r=session.find(a)
>>> print r
SavedQuestionList, len: 172

>>> # find by id non list
>>> a=api.SavedQuestion()
>>> a.id=1
>>> r=session.find(a)
>>> print r
SavedQuestion, name: 'Run Unmanaged Asset Scan on All Machines'

>>> # find by name non list
>>> a=api.SavedQuestion()
>>> a.id=1
>>> r1=session.find(a)
>>> print r1
>>> b=api.SavedQuestion()
>>> b.name=r1.name
>>> r2=session.find(b)
>>> print r2
SavedQuestion, name: 'Run Unmanaged Asset Scan on All Machines'
SavedQuestion, name: 'Run Unmanaged Asset Scan on All Machines'

>>> # findall list
>>> a=api.SavedQuestionList()
>>> r=session.find(a)
>>> print r
SavedQuestionList, len: 172

>>> # find by id in list
>>> a=api.SavedQuestion()
>>> a.id=1
>>> b=api.SavedQuestionList()
>>> b.append(a)
>>> r=session.find(a)
>>> print r
SavedQuestion, name: 'Run Unmanaged Asset Scan on All Machines'

>>> # find by name in list
>>> a=api.SavedQuestion()
>>> a.id=1
>>> r1=session.find(a)
>>> print r1
>>> b=api.SavedQuestion()
>>> b.name=r1.name
>>> c=api.SavedQuestionList()
>>> c.append(b)
>>> r2=session.find(c)
>>> print r2
SavedQuestion, name: 'Run Unmanaged Asset Scan on All Machines'
SavedQuestionList, len: 172
```

## Select

```

>>> # findall non list
>>> a=api.Select()
>>> r=session.find(a)
>>> print r
None

>>> # findall list
>>> a=api.SelectList()
>>> r=session.find(a)
>>> print r
None
```

## Sensor

```

>>> # findall non list
>>> a=api.Sensor()
>>> r=session.find(a)
>>> print r
SensorList, len: 586

>>> # find by id non list
>>> a=api.Sensor()
>>> a.id=1
>>> r=session.find(a)
>>> print r
Sensor, name: 'Action Statuses'

>>> # find by name non list
>>> a=api.Sensor()
>>> a.id=1
>>> r1=session.find(a)
>>> print r1
>>> b=api.Sensor()
>>> b.name=r1.name
>>> r2=session.find(b)
>>> print r2
Sensor, name: 'Action Statuses'
Sensor, name: 'Action Statuses'

>>> # find by hash non list
>>> a=api.Sensor()
>>> a.id=1
>>> r1=session.find(a)
>>> print r1
>>> b=api.Sensor()
>>> b.hash=r1.hash
>>> r2=session.find(b)
>>> print r2
Sensor, name: 'Action Statuses'
Sensor, name: 'Action Statuses'

>>> # findall list
>>> a=api.SensorList()
>>> r=session.find(a)
>>> print r
SensorList, len: 586

>>> # find by id in list
>>> a=api.Sensor()
>>> a.id=1
>>> b=api.SensorList()
>>> b.append(a)
>>> r=session.find(a)
>>> print r
Sensor, name: 'Action Statuses'

>>> # find by name in list
>>> a=api.Sensor()
>>> a.id=1
>>> r1=session.find(a)
>>> print r1
>>> b=api.Sensor()
>>> b.name=r1.name
>>> c=api.SensorList()
>>> c.append(b)
>>> r2=session.find(c)
>>> print r2
Sensor, name: 'Action Statuses'
SensorList, len: 1
```

## SensorQuery

```

>>> # findall non list
>>> a=api.SensorQuery()
>>> r=session.find(a)
>>> print r
None

>>> # findall list
>>> a=api.SensorQueryList()
>>> r=session.find(a)
>>> print r
None
```

## SensorStringHints

```

>>> # findall non list
>>> a=api.SensorStringHints()
>>> r=session.find(a)
>>> print r
None
```

## SensorSubcolumn

```

>>> # findall non list
>>> a=api.SensorSubcolumn()
>>> r=session.find(a)
>>> print r
None

>>> # find by name non list
>>> a=api.SensorSubcolumn()
>>> a.id=1
>>> r1=session.find(a)
>>> print r1
>>> b=api.SensorSubcolumn()
>>> b.name=r1.name
>>> r2=session.find(b)
>>> print r2
None
EXCEPTION:  'NoneType' object has no attribute 'name'

>>> # findall list
>>> a=api.SensorSubcolumnList()
>>> r=session.find(a)
>>> print r
None

>>> # find by name in list
>>> a=api.SensorSubcolumn()
>>> a.id=1
>>> r1=session.find(a)
>>> print r1
>>> b=api.SensorSubcolumn()
>>> b.name=r1.name
>>> c=api.SensorSubcolumnList()
>>> c.append(b)
>>> r2=session.find(c)
>>> print r2
None
EXCEPTION:  'NoneType' object has no attribute 'name'
```

## SoapError

```

>>> # findall non list
>>> a=api.SoapError()
>>> r=session.find(a)
>>> print r
None
```

## SystemSetting

```

>>> # findall non list
>>> a=api.SystemSetting()
>>> r=session.find(a)
>>> print r
SystemSettingsList, len: 88

>>> # find by id non list
>>> a=api.SystemSetting()
>>> a.id=1
>>> r=session.find(a)
>>> print r
SystemSetting, name: 'load_initial_content'

>>> # find by name non list
>>> a=api.SystemSetting()
>>> a.id=1
>>> r1=session.find(a)
>>> print r1
>>> b=api.SystemSetting()
>>> b.name=r1.name
>>> r2=session.find(b)
>>> print r2
SystemSetting, name: 'load_initial_content'
SystemSetting, name: 'load_initial_content'
```

## SystemSettingsList

```

>>> # findall non list
>>> a=api.SystemSettingsList()
>>> r=session.find(a)
>>> print r
SystemSettingsList, len: 88
```

## SystemStatusAggregate

```

>>> # findall non list
>>> a=api.SystemStatusAggregate()
>>> r=session.find(a)
>>> print r
None
```

## SystemStatusList

```

>>> # findall non list
>>> a=api.SystemStatusList()
>>> r=session.find(a)
>>> print r
SystemStatusList, len: 6
```

## UploadFile

```

>>> # findall non list
>>> a=api.UploadFile()
>>> r=session.find(a)
>>> print r
None

>>> # find by id non list
>>> a=api.UploadFile()
>>> a.id=1
>>> r=session.find(a)
>>> print r
None

>>> # find by hash non list
>>> a=api.UploadFile()
>>> a.id=1
>>> r1=session.find(a)
>>> print r1
>>> b=api.UploadFile()
>>> b.hash=r1.hash
>>> r2=session.find(b)
>>> print r2
None
EXCEPTION:  'NoneType' object has no attribute 'hash'

>>> # findall list
>>> a=api.UploadFileList()
>>> r=session.find(a)
>>> print r
None

>>> # find by id in list
>>> a=api.UploadFile()
>>> a.id=1
>>> b=api.UploadFileList()
>>> b.append(a)
>>> r=session.find(a)
>>> print r
None
```

## UploadFileStatus

```

>>> # findall non list
>>> a=api.UploadFileStatus()
>>> r=session.find(a)
>>> print r
None

>>> # find by hash non list
>>> a=api.UploadFileStatus()
>>> a.id=1
>>> r1=session.find(a)
>>> print r1
>>> b=api.UploadFileStatus()
>>> b.hash=r1.hash
>>> r2=session.find(b)
>>> print r2
None
EXCEPTION:  'NoneType' object has no attribute 'hash'
```

## User

```

>>> # findall non list
>>> a=api.User()
>>> r=session.find(a)
>>> print r
EXCEPTION:  ERROR: 400 Bad RequestXML Parse Error: SOAPProcessing Exception: class UserNotFound

>>> # find by id non list
>>> a=api.User()
>>> a.id=1
>>> r=session.find(a)
>>> print r
User, name: 'Jim Olsen'

>>> # find by name non list
>>> a=api.User()
>>> a.id=1
>>> r1=session.find(a)
>>> print r1
>>> b=api.User()
>>> b.name=r1.name
>>> r2=session.find(b)
>>> print r2
User, name: 'Jim Olsen'
EXCEPTION:  ERROR: 400 Bad RequestXML Parse Error: SOAPProcessing Exception: class UserNotFound

>>> # findall list
>>> a=api.UserList()
>>> r=session.find(a)
>>> print r
UserList, len: 3

>>> # find by id in list
>>> a=api.User()
>>> a.id=1
>>> b=api.UserList()
>>> b.append(a)
>>> r=session.find(a)
>>> print r
User, name: 'Jim Olsen'

>>> # find by name in list
>>> a=api.User()
>>> a.id=1
>>> r1=session.find(a)
>>> print r1
>>> b=api.User()
>>> b.name=r1.name
>>> c=api.UserList()
>>> c.append(b)
>>> r2=session.find(c)
>>> print r2
User, name: 'Jim Olsen'
UserList, len: 3
```

## UserPermissions

```

>>> # findall non list
>>> a=api.UserPermissions()
>>> r=session.find(a)
>>> print r
None
```

## UserRole

```

>>> # findall non list
>>> a=api.UserRole()
>>> r=session.find(a)
>>> print r
None

>>> # find by id non list
>>> a=api.UserRole()
>>> a.id=1
>>> r=session.find(a)
>>> print r
None

>>> # find by name non list
>>> a=api.UserRole()
>>> a.id=1
>>> r1=session.find(a)
>>> print r1
>>> b=api.UserRole()
>>> b.name=r1.name
>>> r2=session.find(b)
>>> print r2
None
EXCEPTION:  'NoneType' object has no attribute 'name'

>>> # findall list
>>> a=api.UserRoleList()
>>> r=session.find(a)
>>> print r
UserRoleList, len: 9

>>> # find by id in list
>>> a=api.UserRole()
>>> a.id=1
>>> b=api.UserRoleList()
>>> b.append(a)
>>> r=session.find(a)
>>> print r
None

>>> # find by name in list
>>> a=api.UserRole()
>>> a.id=1
>>> r1=session.find(a)
>>> print r1
>>> b=api.UserRole()
>>> b.name=r1.name
>>> c=api.UserRoleList()
>>> c.append(b)
>>> r2=session.find(c)
>>> print r2
None
EXCEPTION:  'NoneType' object has no attribute 'name'
```

## VersionAggregate

```

>>> # findall non list
>>> a=api.VersionAggregate()
>>> r=session.find(a)
>>> print r
None

>>> # findall list
>>> a=api.VersionAggregateList()
>>> r=session.find(a)
>>> print r
None
```

## WhiteListedUrl

```

>>> # findall non list
>>> a=api.WhiteListedUrl()
>>> r=session.find(a)
>>> print r
EXCEPTION:  ERROR: 400 Bad RequestXML Parse Error: SOAPProcessing Exception: class WhiteListedURLNotFound

>>> # find by id non list
>>> a=api.WhiteListedUrl()
>>> a.id=1
>>> r=session.find(a)
>>> print r
WhiteListedUrl, id: 1

>>> # findall list
>>> a=api.WhiteListedUrlList()
>>> r=session.find(a)
>>> print r
WhiteListedUrlList, len: 3

>>> # find by id in list
>>> a=api.WhiteListedUrl()
>>> a.id=1
>>> b=api.WhiteListedUrlList()
>>> b.append(a)
>>> r=session.find(a)
>>> print r
WhiteListedUrl, id: 1
```

## WhiteListedUrlList

```

>>> # findall non list
>>> a=api.WhiteListedUrlList()
>>> r=session.find(a)
>>> print r
WhiteListedUrlList, len: 3
```

## XmlError

```

>>> # findall non list
>>> a=api.XmlError()
>>> r=session.find(a)
>>> print r
None
```

